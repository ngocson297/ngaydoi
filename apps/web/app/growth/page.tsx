"use client";
import{FormEvent,useCallback,useEffect,useState}from"react";import{AppShell}from"../../components/app-shell";import{AuthGate}from"../../components/auth-gate";import{useAuth}from"../../components/auth-provider";import{ApiError}from"../../lib/api";import type{WeddingSummary}from"../../lib/weddings";
type Ref={id:string;code:string;label:string|null;visitCount:number;signupCount:number;conversionCount:number};type Domain={id:string;hostname:string;status:string;verificationToken:string;dnsTarget:string;weddingTitle:string};
function Content(){const{authRequest}=useAuth();const[weddings,setWeddings]=useState<WeddingSummary[]>([]);const[refs,setRefs]=useState<Ref[]>([]);const[domains,setDomains]=useState<Domain[]>([]);const[code,setCode]=useState("");const[label,setLabel]=useState("");const[weddingId,setWeddingId]=useState("");const[hostname,setHostname]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const load=useCallback(async()=>{const[w,r,d]=await Promise.all([authRequest<WeddingSummary[]>("/weddings"),authRequest<Ref[]>("/growth/referrals"),authRequest<Domain[]>("/growth/domains")]);setWeddings(w);setRefs(r);setDomains(d);if(!weddingId&&w[0])setWeddingId(w[0].id)},[authRequest,weddingId]);useEffect(()=>{void load().catch(e=>setError(e instanceof ApiError?e.message:"Không thể tải Growth Hub"))},[load]);async function referral(e:FormEvent){e.preventDefault();setBusy(true);try{await authRequest("/growth/referrals",{method:"POST",body:JSON.stringify({code,label})});setCode("");setLabel("");await load()}catch(e){setError(e instanceof ApiError?e.message:"Không thể tạo mã")}finally{setBusy(false)}}async function domain(e:FormEvent){e.preventDefault();setBusy(true);try{await authRequest("/growth/domains",{method:"POST",body:JSON.stringify({weddingId,hostname})});setHostname("");await load()}catch(e){setError(e instanceof ApiError?e.message:"Không thể gửi tên miền")}finally{setBusy(false)}}return <AppShell active="growth"><div className="page-heading"><div><p className="eyebrow">GROWTH HUB</p><h1>Chia sẻ và xây dựng thương hiệu riêng</h1><p>Mã giới thiệu minh bạch, tên miền riêng có hướng dẫn từng bước.</p></div></div>{error&&<div className="form-alert error">{error}</div>}<div className="support-layout"><section className="panel"><h2>Mã giới thiệu</h2><form className="pilot-form" onSubmit={referral}><label>Mã mong muốn<input required minLength={4} value={code} onChange={e=>setCode(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,""))} placeholder="NGAYDOI2026"/></label><label>Nhãn nội bộ<input value={label} onChange={e=>setLabel(e.target.value)} placeholder="Studio Hoa Hồng"/></label><button className="btn btn-primary" disabled={busy}>Tạo mã</button></form><div className="growth-list">{refs.map(r=><article key={r.id}><div><strong>{r.code}</strong><p>{r.label||"Mã cá nhân"}</p></div><div className="mini-metrics"><span>{r.visitCount} lượt xem</span><span>{r.signupCount} đăng ký</span><span>{r.conversionCount} mua gói</span></div></article>)}</div></section><section className="panel"><h2>Tên miền riêng</h2><form className="pilot-form" onSubmit={domain}><label>Wedding<select required value={weddingId} onChange={e=>setWeddingId(e.target.value)}>{weddings.map(w=><option key={w.id} value={w.id}>{w.title}</option>)}</select></label><label>Tên miền<input required value={hostname} onChange={e=>setHostname(e.target.value)} placeholder="thiepcuoi.tenban.vn"/></label><button className="btn btn-primary" disabled={busy||!weddingId}>Gửi yêu cầu</button></form>{domains.map(d=><article className="domain-card" key={d.id}><div><strong>{d.hostname}</strong><span className={`status-pill ${d.status.toLowerCase()}`}>{d.status}</span></div><p>{d.weddingTitle}</p><ol><li>Tạo bản ghi CNAME trỏ tới <code>{d.dnsTarget}</code></li><li>Tạo TXT <code>_ngaydoi</code> với giá trị <code>{d.verificationToken}</code></li><li>Chờ Admin xác minh và kích hoạt HTTPS.</li></ol></article>)}</section></div></AppShell>}
export default function Page(){return <AuthGate><Content/></AuthGate>}
